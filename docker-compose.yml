version: "3.9"
services:
  rag-agent:
    build: .
    ports:
      - "5000:5000"
    environment:
      RAG_HOST: 0.0.0.0
      RAG_PORT: 5000
      RAG_DEBUG: "false"
      GROQ_API_KEY: ${GROQ_API_KEY}
    volumes:
      - ./data:/app/data
